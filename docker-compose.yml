version: '3.8'
services:
  db:
    container_name: pg_container
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: mhtorneria
      POSTGRES_PASSWORD: Silmampabas6245.
      POSTGRES_DB: name_db
    ports:
      - "5432:5432"
  pgadmin:
    container_name: pgadmin4_container
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: mh.torneria@gmail.com
      PGADMIN_DEFAULT_PASSWORD: Silmampabas6245.
    ports:
      - "5050:80"
